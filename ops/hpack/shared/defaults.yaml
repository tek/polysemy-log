<<: !include extensions.yaml

dependencies:
  - name: base
    version: '>= 4 && < 5'
    mixin:
      - hiding (Prelude)
  - polysemy >= 1.5
  - incipit-core >= 0.1.0.1

ghc-options:
  - -Wall
  - -Wredundant-constraints
  - -Wunused-packages
  - -Widentities
